camera.origin.x = 2.0;
camera.origin.z = 3.0;
camera.fov = 50.0;

settings.background = F3(0.0, 0.0, 0.0);
settings.antialias = 3;
settings.width = 200;
settings.height = 200;
settings.opacity = 0.0;

let light = PointLight();
light.position = F3(3.0, 3.0, 5.0);
light.intensity = 1.5;

let sphere1 = Sphere(1.2);
sphere1.material.rgb = F3(0.3, 0.3, 0.3);

let sphere2 = Sphere(1.2);
sphere2.position.x = 1.0;

sphere1 -= sphere2;

let sphere3 = sphere2;